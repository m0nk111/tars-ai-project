#!/bin/bash
# Frontend reload bij backend restart (placeholder, browser reload via websocket trigger)
# Voeg in app.js toe:
# socket.addEventListener('close', function() { location.reload(); });
echo "Frontend reload wordt automatisch getriggerd bij backend restart via WebSocket."
